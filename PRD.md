# RhythmTrade - 产品需求文档

> **计划你的交易，交易你的计划**

## 1. 产品定位

### 1.1 一句话描述

一个帮助交易者建立交易纪律的工具——提前制定计划，强制复盘总结，数据存储在用户自己的飞书文档中。

### 1.2 核心问题

交易者最大的敌人是**情绪化交易**：
- 没有计划就开仓
- 开仓后随意修改止损止盈
- 平仓后不复盘，重复犯错

### 1.3 解决方案

强制工作流：**计划 → 执行 → 复盘**

- 开仓前必须写清楚：为什么进？在哪出？
- 平仓后必须记录：结果如何？学到什么？
- 数据按周组织，方便回顾交易节奏

---

## 2. 用户画像

### Phase 1: 自己
- 验证核心流程是否顺手

### Phase 2: 小圈子（10-50人）
- 收集反馈，打磨体验

### Phase 3: 产品化（待定）
- 根据 Phase 2 反馈决定

---

## 3. 核心功能

### 3.1 飞书文档集成

**数据归属原则**：用户的数据存储在用户自己的飞书空间，App 不存储任何交易数据。

| 功能 | 说明 |
|------|------|
| OAuth 登录 | 飞书账号授权 |
| 文档读写 | 通过飞书开放 API 操作文档 |
| 自动创建周报 | 每周一个文档，格式统一 |

**文档结构设计**：

```
📁 RhythmTrade/
  📄 2024-W01 交易周报
  📄 2024-W02 交易周报
  📄 2024-W03 交易周报
  ...
```

### 3.2 交易计划

开仓前必须填写：

| 字段 | 必填 | 说明 |
|------|------|------|
| 标的 | ✅ | BTC/ETH/AAPL/沪深300... |
| 方向 | ✅ | 做多/做空 |
| 进场理由 | ✅ | 技术面/基本面/消息面，写清楚逻辑 |
| 入场价格 | ✅ | 计划入场价 |
| 止损价格 | ✅ | 必须有止损 |
| 止盈目标 | ✅ | 至少一个目标价 |
| 仓位大小 | ⬜ | 可选，记录仓位管理 |
| 风险备注 | ⬜ | 可选，潜在风险点 |

### 3.3 持仓跟踪

计划创建后的状态流转：

```
[计划中] → [已开仓] → [已平仓]
              ↓
         [未触发作废]
```

**跨周持仓处理**：
- 上周未平仓的单子，自动显示在本周开头
- 标记为「延续持仓」，保持原有计划信息

### 3.4 复盘总结

平仓后必须填写：

| 字段 | 必填 | 说明 |
|------|------|------|
| 实际出场价 | ✅ | 真实平仓价格 |
| 盈亏结果 | ✅ | 自动计算或手动填写 |
| 执行评分 | ✅ | 计划执行度 1-5 分 |
| 复盘笔记 | ✅ | 做对了什么？做错了什么？ |

**周度总结**（每周日/周一生成）：
- 本周交易次数
- 胜率统计
- 最大单笔盈亏
- 本周教训总结（手动填写）

---

## 4. 技术架构

### 4.1 极简原则

```
┌─────────────────────────────────────────────────┐
│                   Vercel                        │
│  ┌──────────────┐      ┌──────────────────┐    │
│  │   React App  │      │ Serverless Func  │    │
│  │  (静态托管)   │      │ (OAuth Token)    │    │
│  └──────┬───────┘      └────────┬─────────┘    │
└─────────┼──────────────────────┼───────────────┘
          │                      │
          │   ┌──────────────────┘
          ▼   ▼
    ┌─────────────┐
    │  飞书 API   │
    │ (文档读写)   │
    └─────────────┘
          │
          ▼
    ┌─────────────┐
    │ 用户飞书空间 │
    │  (数据存储)  │
    └─────────────┘
```

### 4.2 技术选型

| 层级 | 技术 | 理由 |
|------|------|------|
| 前端框架 | React 18 | 生态成熟 |
| 样式方案 | TailwindCSS | 快速开发 |
| 构建工具 | Rspack | 快，兼容 webpack 生态 |
| 部署平台 | Vercel | 免费，自动化 CI/CD |
| 后端函数 | Vercel Serverless | 仅用于 OAuth token 交换 |
| 数据存储 | 飞书文档 | 用户数据归用户所有 |

### 4.3 为什么不需要传统后端

| 功能 | 方案 |
|------|------|
| 用户认证 | 飞书 OAuth（前端跳转，Serverless 换 token）|
| 数据存储 | 飞书文档 API（用户授权后前端直连）|
| 数据分析 | 前端计算（数据量小，无需后端聚合）|

**唯一需要服务端的地方**：OAuth 的 `client_secret` 不能暴露在前端，需要一个 Serverless Function 做 token 交换。这是一个 20 行代码的函数。

---

## 5. 页面结构

```
/                     # 首页（未登录展示介绍，已登录跳转周报）
/auth/callback        # 飞书 OAuth 回调
/week/:weekId         # 周报页面（本周交易计划列表）
/plan/new             # 新建交易计划
/plan/:planId         # 计划详情/编辑/复盘
/review/:weekId       # 周度复盘总结
```

---

## 6. 飞书文档格式设计

每个周报文档的 Block 结构：

```markdown
# 2024-W03 交易周报

## 📊 本周概览
- 交易次数：5
- 已平仓：3（胜率 66.7%）
- 持仓中：2

---

## 📈 交易记录

### #1 BTC 做多 ✅ 已平仓
**状态**：已平仓 | 盈利 +2.3%

| 计划 | 实际 |
|------|------|
| 入场：42000 | 入场：42150 |
| 止损：41000 | - |
| 止盈：45000 | 出场：44800 |

**进场理由**：
日线突破前高，4H MACD 金叉确认...

**复盘笔记**：
执行评分：4/5
做对了：严格按计划入场
做错了：止盈位置保守了，可以拿到 45500

---

### #2 ETH 做空 🔴 持仓中
**状态**：持仓中 | 浮亏 -1.2%

（延续上周 W02 计划）

...

---

## 📝 本周总结
（手动填写）
```

---

## 7. MVP 范围

### ✅ Phase 1 包含
- [ ] 飞书 OAuth 登录
- [ ] 自动创建/读取周报文档
- [ ] 新建交易计划（写入飞书文档）
- [ ] 查看本周计划列表
- [ ] 更新计划状态（开仓/平仓）
- [ ] 填写复盘笔记
- [ ] 基础周度统计

### ❌ Phase 1 不包含
- 持仓自动同步（MT4/币圈/股票）
- 多用户协作
- 高级数据分析
- 移动端适配（先做好桌面端）

---

## 8. 风险与约束

| 风险 | 应对 |
|------|------|
| 飞书 API 限流 | 单用户调用频率低，暂不考虑 |
| 文档格式解析 | 定义严格的 Block 结构，用 ID 定位 |
| Token 过期 | 前端检测 401，引导重新授权 |
| 飞书文档被手动修改 | 做好容错，解析失败时提示用户 |

---

## 9. 后续迭代方向（Phase 2+）

1. **持仓同步**：对接交易所 API，自动读取开仓信息
2. **行情集成**：显示当前价格，计算浮盈浮亏
3. **交易日历**：可视化交易频率和盈亏分布
4. **AI 复盘助手**：基于历史记录生成复盘建议
5. **小圈子分享**：允许分享周报给指定好友

---

## 10. 项目命名

**RhythmTrade** - 节奏交易

含义：
- Rhythm（节奏）：交易需要节奏感，计划-执行-复盘的循环
- Trade（交易）：核心场景
- 谐音：有「韵律」的感觉，暗示纪律性

备选：
- TradePlan
- PlanTrade
- TradeLog

---

*文档版本：v0.1*
*最后更新：2024-01*
